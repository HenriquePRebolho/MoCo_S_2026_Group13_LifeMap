package com.example.livemap.ui.theme

import androidx.compose.ui.graphics.Color

// --- LiveMap color palette ---

// Primary green gradient endpoints
// Used for main buttons, active navigation, and accent elements
val GreenLight = Color(0xFF9ED6A9)
val GreenDark = Color(0xFF7BC88F)

// Warm background gradient endpoints
// Used as the app's main background
val BackgroundWarm = Color(0xFFF5EEE6)
val BackgroundGold = Color(0xFFFFF4D6)

// Category colors — used to tag different event types
val CategorySport = Color(0xFF9ED6A9)  // Green
val CategoryArt = Color(0xFFFFB6C1)    // Pink
val CategoryFun = Color(0xFFFFD966)    // Yellow
val CategoryStudy = Color(0xFFA8D5E2)  // Blue
val CategoryFood = Color(0xFFE6B8E6)   // Purple

// Neutral tones for text and surfaces
val TextPrimary = Color(0xFF1F2937)    // Dark gray, almost black
val TextSecondary = Color(0xFF6B7280)  // Medium gray
val SurfaceWhite = Color(0xFFFFFFFF)
val BorderLight = Color(0xFFE5E7EB)